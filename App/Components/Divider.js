import React from 'react'
import { View, StyleSheet } from 'react-native'

export default () => <View style={styles.divider} />

const styles = StyleSheet.create({
  divider: {
    backgroundColor: '#9f9da1',
    height: 1,
  }
})
